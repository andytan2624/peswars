'use strict';

import React from 'react';

const Footer = React.createClass({

  render() {
    return (
        <footer>This site is a work in progress and is currently using some new technologies (purposely) that haven't been implemented across all browsers. Please view in Chrome for best results. See Steve for ideas and updates...</footer>
    );
  }

});

export default Footer;
